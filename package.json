{
  "dependencies": {
    "reveal.js": "^5.2.1",
    "vite": "^6.3.5"
  },
  "scripts": {
    "dev": "npm-run-all --parallel watch:theme vite",
    "vite": "vite",
    "watch:theme": "sass --watch css/source:css --load-path=node_modules",
    "build:theme": "sass css/source:css --load-path=node_modules",
    "build": "npm-run-all build:theme vite:build",
    "vite:build": "vite build",
    "make": "node scripts/make-presentation.js"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.89.1"
  }
}
