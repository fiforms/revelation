// Default location — remote broker is embedded in the Vite server on the same port

window.revealRemoteServer = window.location.protocol + "//" + window.location.host + "/";
window.presenterPluginsPublicServer = "https://revealremote.fiforms.org/presenter-plugins-socket";
